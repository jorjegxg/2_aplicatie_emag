# Postgres (Docker in WSL)
POSTGRES_USER=emag
POSTGRES_PASSWORD=emag
POSTGRES_DB=emag

# Node app on Windows → Postgres published on localhost:5432
DATABASE_URL=postgres://emag:emag@127.0.0.1:5432/emag
